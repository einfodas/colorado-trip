import WeatherPage from "@/components/weather/WeatherPage";

export const metadata = {
  title: "Weather — Colorado Family Vacation",
  description: "Weather forecast and conditions for each day of the Colorado trip.",
};

export default function Weather() {
  return <WeatherPage />;
}
